export default class NewsCardList {

    constructor(container) {
        this.container = container
    }

    pushCard(elements) {
        elements.forEach(element => {
            //загружаем в контейнер три карточки
        });
    }

}